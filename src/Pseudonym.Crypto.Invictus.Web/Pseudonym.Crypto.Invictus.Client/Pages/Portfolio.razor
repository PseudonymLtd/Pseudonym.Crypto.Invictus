@page "/portfolio"
@inject IUserSettings UserSettings
@inject IApiClient Client
@inject IAppState AppState

<h1 class="display-3">Portfolio Overview</h1>
@if (!UserSettings.HasValidAddress())
{
    <WalletEntry />
}
else if (!investments.Any())
{
    <Loading />
}
else
{
    <h2 class="portfolio-address">
        <EthAddress Address="@UserSettings.WalletAddress" FontSize="28" />
        <small class="text-muted text-align-mob-center">(@investments.Count) Funds</small>
    </h2>
    <div class="alert alert-secondary mt-4" role="alert">
        <span class="oi oi-info mr-2" aria-hidden="true"></span>
        <span class="text-wrap">
            Below shows the snapshot of your portfolio performance as of right now.
        </span>
    </div>
    <div class="container-fluid" style="max-width: none">
        <div class="row">
            <div class="col-xs-auto col-sm-auto col-md-auto col-lg-auto col-xl-auto" style="min-width: 200px; padding-bottom: 15px;">
                <small style="font-weight: bold;">Total Net Value (@UserSettings.CurrencyCode.GetSymbol())</small>
                <div>
                    <Money Value="TotalAssetValue" Decimals="2" CurrencyCode="UserSettings.CurrencyCode" />
                </div>
            </div>
            <div class="col-xs-auto col-sm-auto col-md-auto col-lg-auto col-xl-auto" style="min-width: 200px; padding-bottom: 15px;">
                <small style="font-weight: bold;">Total Market Value (@UserSettings.CurrencyCode.GetSymbol())</small>
                <div>
                    <Money Value="TotalMarketValue" Decimals="2" CurrencyCode="UserSettings.CurrencyCode" />
                </div>
            </div>
        </div>
    </div>
    <h2 class="display-4">Portfolio Distribution</h2>

    <PortfolioDistributionChart Investments="investments" IsHollow="false" />

    <h2 class="display-4">Invested Funds</h2>
    <div class="container" style="display: contents;">
        @foreach (var investment in investments.OrderBy(x => x.RealValue))
        {
            <div class="row">
                <div class="col">
                    <InvestmentSummary Investment="investment" CurrencyCode="UserSettings.CurrencyCode" Clickable="true" />
                </div>
            </div>
        }
    </div>
}


@code {
    private readonly List<ApiInvestment> investments;

    public Portfolio()
    {
        investments = new List<ApiInvestment>();
    }

    protected decimal TotalAssetValue => investments.Sum(x => x.RealValue);

    protected decimal TotalMarketValue => investments.Sum(x => x.MarketValue ?? 0);

    protected override async Task OnInitializedAsync()
    {
        AppState.Assign(async () => await DrawAsync(false));

        await DrawAsync(true);
    }

    private async Task DrawAsync(bool firstRender)
    {
        if (!firstRender)
        {
            investments.Clear();

            StateHasChanged();
        }

        if (UserSettings.HasValidAddress())
        {
            await foreach (var item in Client.ListInvestmentsAsync())
            {
                investments.Add(item);
            }

            StateHasChanged();
        }
    }
}